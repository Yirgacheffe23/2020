#include<iostream>
using namespace std;

int main()
{
	////const int p = 10;
	////const int * vp = &p;
	//////void *vp = &p; //error
	//const int*ptr;
	//int  val = 3;
	//ptr = &val;

	//int *ptr1 = &val;
	//*ptr1 = 4;
	//cout << *ptr << endl;
	////允许把非const对象的地址赋值给const对象的指针
	////如果要修改指针所指向的对象值，必须通过其他方式修改
	////不能直接通过当前指针直接修改


	int num = 0;
	int * const ptr = &num; //const指针必须初始化！且const指针的值不能修改
	num = 1;
	//int * t = &num;
	//*t = 1;
	cout << *ptr << endl;
	system("pause");
	return 0;
}
