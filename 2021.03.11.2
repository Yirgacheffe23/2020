#include<iostream>
using namespace std;

int test(int n)
{
	int count = 0;
	while (n)
	{
		if (n & 1)
			count++;

		n = n >> 1;
	}
	return count;
}

int main()
{
	for (int i = 0; i < 13; i++)
	{
		cout << "Num of 1 in "<< i <<" : "<< test(i) << endl;
	}
	system("pause");
	return 0;
}
